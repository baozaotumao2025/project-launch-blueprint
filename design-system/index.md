# Design System

> 第五阶段目录：只负责 `api contract map + api-contract validation report + upstream input inventory + 辅助证据 -> design tokens -> components -> states -> layouts -> accessibility / responsive / motion rules -> design-system validation report`。
> 这一层的职责是把已验证契约翻译成跨切片一致的 UI 语言，让视觉和交互复用而不是每页重写。

## Files

- `method.md`
- `validation-rules.md`
- `prompt-templates.md`
- `output-schema.md`
- `example-output.md`
- `checklist.md`

## Start Here

1. `../index.md`
2. `method.md`

> 本目录消费 `api-contract` 的已验证输出，并冻结上游输入 inventory / coverage matrix，不直接把原始 `analysis` 当作主输入；`analysis` 只作为辅助证据。
